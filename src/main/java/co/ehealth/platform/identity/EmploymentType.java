package co.ehealth.platform.identity;

// Matches the legacy Staff/insertstaff form's "Employment Status" options.
public enum EmploymentType {
    PERMANENT,
    CONTRACT,
    INTERN,
    COMMUNITY_SERVICE,
    EXTENDED_PUBLIC_WORKS,
    SECONDED
}
